# Notion CMS（PRD F-07、D-05）。複製成 .env 後填入 NOTION_TOKEN；.env 不進版控。
#
# 什麼是 integration：
#   它不是 Notion 裡的一頁，是「一把讓程式讀你資料庫的鑰匙」，
#   建在你帳號的開發者設定裡，跟工作區的頁面無關。不用在留白髮所底下新增任何東西。
#
# 步驟一：建鑰匙
#   到 https://app.notion.com/developers/connections
#   （舊網址 notion.so/my-integrations 會轉到這裡；也可以從 Notion 左下
#     設定 → 我的連結／Connections → 開發或管理整合 進去）
#   新建一個 internal integration，名字隨便取（例如 margin-web），
#   工作區選「huang way's Notion」，然後複製它的密鑰貼到下面 NOTION_TOKEN。
#   密鑰是 ntn_ 或 secret_ 開頭的一長串。
#
# 步驟二：把鑰匙給三個資料庫（漏掉這步 API 會回 404，因為它看不到那幾頁）
#   打開 Notion 的「留白髮所 MARGIN — 網站 CMS」，
#   底下三個資料庫（髮型誌 Journal／作品集 Works／設計師 Stylists）各自開啟，
#   右上角 ⋯ → 連結／Connections → 加入步驟一建的那個 integration。
#   偷懶做法：直接對「留白髮所 MARGIN — 網站 CMS」這一頁加，子頁面會繼承。
#
# 步驟三：cp .env.example .env，填好 NOTION_TOKEN，跑 npm run sync:notion
#
# 下面三個 ID 已經對應到現有的資料庫，不用改。
NOTION_TOKEN=
NOTION_DB_JOURNAL=296bf8f422264c4fa24c6cc54568ca5d
NOTION_DB_WORKS=469232f50f9942b287a0b0f6290b55f6
NOTION_DB_STYLISTS=abba30cba3234b6aa4d7e643c2648561

# ─────────────────────────────────────────────────────────────
# 預約系統：Google 日曆（PRD F-06、D-12）
#
# 設計師本來就在用日曆，所以不另外養一套預約後台：
# 空檔就是日曆上沒事的時段，預約就是日曆上多一個活動。
# 店家要看今天有誰要來，打開自己的 Google 日曆就好。
#
# 步驟一：建一個「機器人帳號」（service account）
#   到 https://console.cloud.google.com → 建立專案（名字隨便，例如 margin-web）
#   左側「API 和服務」→「程式庫」→ 搜尋 Google Calendar API → 啟用
#   左側「IAM 與管理」→「服務帳戶」→ 建立服務帳戶 → 名字隨便 → 建立
#   點進去 →「金鑰」分頁 → 新增金鑰 → 建立新的金鑰 → 選 JSON → 下載
#
# 步驟二：把 JSON 裡的兩個值填到下面
#   client_email  → NUXT_GOOGLE_SA_EMAIL
#   private_key   → NUXT_GOOGLE_SA_KEY（整段貼上，含 BEGIN／END 兩行，
#                   換行維持成字面上的 \n，整個值用雙引號包起來）
#
# 步驟三：把日曆分享給那個機器人帳號
#   每位設計師的 Google 日曆 → 設定與共用 → 與特定使用者或群組共用 →
#   加入步驟二那個 client_email，權限選「變更活動」
#   ★ 漏掉這步 API 不會報錯，那位設計師會安靜地退回示範空檔
#   同一頁往下捲有「日曆 ID」，貼到下面對應的變數
#
# 步驟四：npm run dev，打開 http://127.0.0.1:3000/api/booking/check
#   它會列出哪幾位接上了、哪幾位讀不到，以及讀不到要去哪裡修
#
# 全部留空的話 /booking 退回站上的示範空檔，流程仍然走得完。
#
# ★ 這一段以下的名字都要 NUXT_ 開頭，不能省。
#   Nuxt 只把 NUXT_ 開頭的環境變數對進 nuxt.config 的 runtimeConfig
#   （NUXT_GOOGLE_SA_EMAIL → googleSaEmail），少了前綴會被安靜忽略，
#   然後你會看到「明明填了卻還是走示範資料」。
#   上面 Notion 那幾個沒有前綴，是因為它們給 npm run sync:notion 這支
#   建置前腳本用的，那支直接讀 process.env，不經過 runtimeConfig。
NUXT_GOOGLE_SA_EMAIL=
NUXT_GOOGLE_SA_KEY=
NUXT_GCAL_SHU=
NUXT_GCAL_YUKI=
NUXT_GCAL_RAY=
NUXT_GCAL_AN=
# 取消／改期的說明頁或表單網址。留空的話信件與完成頁改成請顧客來電 07-338-0088。
NUXT_BOOKING_CANCEL_URL=

# ─────────────────────────────────────────────────────────────
# 寄信 Resend（PRD F-10 預約完成信、F-11 徵才通知信）
#
# 到 https://resend.com 註冊 → API Keys → Create，複製那把 key。
#
# 還沒有自己的網域也能測：NUXT_MAIL_FROM 用 Resend 的沙盒寄件人
# onboarding@resend.dev，限制是只能寄到你註冊 Resend 用的那個信箱。
# 之後在 Resend 的 Domains 驗證自己的網域，再把 NUXT_MAIL_FROM 換掉就好，程式不用動。
#
# 免費額度每月 3,000 封、每天 100 封。
# NUXT_RESEND_API_KEY 或 NUXT_MAIL_FROM 留空時完全不寄信，
# 預約與應徵仍然送得出去，畫面上會照實說明沒有寄出確認信。
NUXT_RESEND_API_KEY=
NUXT_MAIL_FROM=MARGIN <onboarding@resend.dev>
# 店內收件匣，新預約與應徵通知都寄到這裡。留空則只寄給顧客。
NUXT_MAIL_INBOX=
