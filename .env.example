# Notion CMS（PRD F-07、D-05）。複製成 .env 後填入實際值；.env 不進版控。
#
# 1. 到 https://www.notion.so/my-integrations 建一個 internal integration，
#    複製它的 Internal Integration Secret 貼到 NOTION_TOKEN。
# 2. 到 Notion 上「留白髮所 MARGIN — 網站 CMS」底下的三個資料庫，
#    每一個都用右上角 ⋯ → Connections → 加入剛剛那個 integration。
#    沒做這步的話 API 會回 404，因為 integration 看不到那頁。
#
# 這三個 ID 已對應到現有的資料庫，正常情況不用改。
NOTION_TOKEN=
NOTION_DB_JOURNAL=296bf8f422264c4fa24c6cc54568ca5d
NOTION_DB_WORKS=469232f50f9942b287a0b0f6290b55f6
NOTION_DB_STYLISTS=abba30cba3234b6aa4d7e643c2648561

# ─────────────────────────────────────────────────────────────
# 預約系統 SimplyBook.me（PRD F-06、D-02）
#
# 到 SimplyBook 後台 Custom Features → API 打開 API，
# 那一頁上有 Company login 與 API key。
# 兩個都留空的話 /booking 會退回站上的示範空檔，流程仍然走得完。
#
# 填好之後，開發模式下打 http://127.0.0.1:3000/api/booking/catalog
# 會列出後台的 service 與 provider id，照著填進
# server/utils/simplybook.map.ts 的對照表。沒對照到的項目一樣走示範資料。
SIMPLYBOOK_LOGIN=
SIMPLYBOOK_API_KEY=
# 取消／改期連結樣板，{id} 與 {hash} 會被換成 SimplyBook 回傳的值。
# 留空的話信件與完成頁改成請顧客來電 07-338-0088。
SIMPLYBOOK_CANCEL_URL=

# ─────────────────────────────────────────────────────────────
# 寄信 Resend（PRD F-10 預約完成信、F-11 徵才通知信）
#
# 到 https://resend.com/api-keys 建一把 key，
# 並在 Domains 驗證好寄件網域 —— 沒驗證的網域寄出去會進垃圾信。
# RESEND_API_KEY 或 MAIL_FROM 留空時完全不寄信，
# 預約與應徵仍然送得出去，畫面上會照實說明沒有寄出確認信。
RESEND_API_KEY=
MAIL_FROM=MARGIN 留白髮所 <hello@example.com>
# 店內收件匣，新預約與應徵通知都寄到這裡。留空則只寄給顧客。
MAIL_INBOX=
